apiVersion: apps/v1
kind: Deployment
metadata:
  name: laboratory-service
  labels:
    app: laboratory-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: laboratory-service
  template:
    metadata:
      labels:
        app: laboratory-service
    spec:
      containers:
      - name: laboratory-service
        image: hospital-management-system/laboratory-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3007
        envFrom:
        - configMapRef:
            name: hms-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: LABORATORY_DB_URL
---
apiVersion: v1
kind: Service
metadata:
  name: laboratory-service
spec:
  selector:
    app: laboratory-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3007
