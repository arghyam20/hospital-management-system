apiVersion: apps/v1
kind: Deployment
metadata:
  name: pharmacy-service
  labels:
    app: pharmacy-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pharmacy-service
  template:
    metadata:
      labels:
        app: pharmacy-service
    spec:
      containers:
      - name: pharmacy-service
        image: hospital-management-system/pharmacy-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3006
        envFrom:
        - configMapRef:
            name: hms-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: PHARMACY_DB_URL
---
apiVersion: v1
kind: Service
metadata:
  name: pharmacy-service
spec:
  selector:
    app: pharmacy-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3006
